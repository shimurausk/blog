<%= form_for ([@article,@article.comments.build]) do |f| %>
  <p>
  	<%= f.label :commenter, "Commenter" %>
  	<%= f.text_field :commenter %>
  </p>
  <p>
  	<%= f.label :body, "Commenter" %>
  	<%= f.text_area :body %>
  </p>
  <%= f.submit "Submit" %>
<% end -%>