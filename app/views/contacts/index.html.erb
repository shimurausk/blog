<%= render 'articles/header' %>
<section>
  <div class="contents contact pure-g">
    <div class="pure-u-1">
    	<h4 class="tbar">お問い合わせ</h4>
			<p>当サイトをご覧いただきありがとうございます。不明点ございましたら、どうぞお気軽にお問い合わせください。</p>
			<div class="note">※回答には多少のお日にちをいただく事があります。予めご了承下さいますようお願い致します。</div>

			<%= form_for @contact,:url => contacts_confirm_path,:html => {:class => 'contact_form'} do |f| %>

			<% if @contact.errors.any? %>
			<div class="errors">
				<strong>エラーがあります</strong>
					<ul>
					<% @contact.errors.each do |attr,msg| %>
						<li><%= msg %></li>
					<% end %>
					</ul>
				</div>
			<% end %>

			<dl id="name" class="pure-g">
			  <dt class="pure-u-1 pure-u-md-4-24"><%= f.label :name, "お名前" %></dt>
			  <dd class="pure-u-1 pure-u-md-20-24"><%= f.text_field :name, class: "form-control" %></dd>
			  <dt class="pure-u-1 pure-u-md-4-24"><%= f.label :email, "メールアドレス" %><span>(半角)</span></dt>
			  <dd class="pure-u-1 pure-u-md-20-24"><%= f.text_field :email, class: "form-control" %></dd>
			  <dt class="pure-u-1 pure-u-md-4-24"><%= f.label :message, "お問い合わせ内容" %></dt>
			  <dd class="pure-u-1 pure-u-md-20-24"><%= f.text_area :message, class: "form-control" %></dd>
			</dl>
			<div class="submit pure-u-1 tac"><%= f.submit "この内容で送信する",class: "pure-button" %></div> 

			<% end %>
    </div>
  </div>
</section>
<%= render 'articles/footer' %>
